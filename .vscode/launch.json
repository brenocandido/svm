{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch executable",
      "type": "cppdbg",
      "request": "launch",
      "cwd": "${workspaceRoot}",
      "program": "${workspaceRoot}/main.exe",
      "preLaunchTask": "Make build",
      "stopAtEntry": true
    },
  ]
}